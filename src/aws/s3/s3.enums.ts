export enum S3FileCategory {
  FILE = 'FILE',
}

export enum FileActions {
  putObject = 'putObject',
  getObject = 'getObject',
}

export enum BucketType {
  private = 'private',
  public = 'public',
}
